# MaskFactory — fully updated pursuing goal

Updated authority: 2026-07-28

## Active pursuing goal — full attribution, blocker reconciliation, and E2E completion

Continue autonomously until the recovered-work authority and the complete
MaskFactory product are both genuinely closed. Do not stop at repository
cleanliness, a large static-test count, or a generated tracker report.

First, finish the finite authority-correction gate:

1. correct every stale repository/current-task topology statement;
2. extend every one of the 4,008 historical-stash rows with exact
   `plan_refs`, `item_ids`, `tracker_ids`, dependency/verify bindings,
   evidence/reconstruction locators, canonical/rollback locations,
   limitations, and completion effect;
3. prove every referenced Item exists and every adopted capability is
   represented by the governing Plan, Item metadata, tracker state, tests,
   and evidence;
4. adjudicate `MF-P6-20.02`, `.03`, and `.04` against their exact verify
   clauses, granting only the tier already proved and recording remaining
   lint, external-prerequisite, runtime-health, exact-runtime, and shutdown
   work honestly; and
5. regenerate and validate the tracker/report/frozen authorities without
   replaying stale state or granting product credit from preservation alone.

Authority-correction progress at 2026-07-28T21:27:49Z: all 4,008 rows now
pass the required row-level bindings with registry SHA-256
`a4436d4a17c7d37990e15c47623b3d51899e1ecba9f7db0b4f9507003dc54930`.
The tracker has been adjudicated without false closure:
`MF-P6-20.02` is `in_progress` 90%, `.03` is `partially_complete` 80%, and
`.04` is `partially_complete` 65%. The finite release still requires commit,
push, clean parity, and a terminal receipt; the remaining acceptance clauses
continue in the product phase below.

Then complete the actual product dependency graph:

- resolve all 34 unfinished `core_autonomous_runtime` dependencies, including
  the 22 unresolved hard blockers in that profile;
- give the 31 remaining portfolio hard blockers outside all completion
  profiles an explicit governed terminal disposition—implemented, retained
  portfolio work, deliberately deferred, or completion-policy reclassified
  through the self-hashed registry—never silent deletion or bulk completion;
- qualify `MF-P4-11.23` and `MF-P4-12.11` with real data and qualified visual
  authority;
- finish `MF-P6-17.03/.04`, `MF-P6-18.*`, the one governed 100-mask campaign
  `MF-P6-19.03`, and the three consecutive mixed campaigns plus immutable
  throughput packet in `MF-P6-19.04`;
- finish canonical test/runtime closure, compact evidence reconstruction,
  disagreement/abstention safety, immutable release, pinned Main/ComfyUI
  adoption, live bounded masking E2E, restart/replay/invalidation/rollback,
  and final byte-identical acceptance; and
- keep local/remote Git, Plan, Items, Instructions, tracker/DAG, reports,
  pursuing goal, current task, repository map, recovery ledgers, and both
  coordinating tasks synchronized throughout.

Every RunPod GPU child must use the shared FIFO lease launcher. Never EC2.
Never use text-only reasoning to approve masks. Never rerun accepted
`MF-P6-19.01` or `.02`. Continue CPU-safe work when a runtime lane is queued
or unavailable. The completion claims remain
`SELF_HOSTED_AUTONOMOUS_LLM_THROUGHPUT_INCOMPLETE` and
`ULTIMATE_MASKING_SYSTEM_E2E_INCOMPLETE` until their exact terminal gates
pass.

The pre-change authority-correction checkpoint is
`C:\MaskFactory_TierA_Backups\authority_attribution_before_change_20260728T201447Z`:
17 payloads / 8,472,585 bytes, zero verification failures,
`SHA256SUMS.tsv` SHA-256
`c3cece21605225ea35b6467bf8b35e4fc4819b4d683df8335ef8d063866e4f08`.

## 0A. Completed foundation: stash/reflog and physical authority convergence

The branch/worktree/directory consolidation passed, but the terminal audit
found five live historical stashes and one generated pre-commit cache that
were outside the earlier path ledger. The recovered functional cohort,
exhaustive stash matrix, Plan/Item/Instruction/Tracker bindings, complete
tests, clean export, zero-residue status, receipt, and local/remote parity now
close this foundation. The pursuing goal continues with the remaining honest
product/runtime/visual/campaign/release/ComfyUI-adoption blockers.

Current phase state:

- the 137 meaningful source/control files have exact semantic dispositions
  with zero unresolved rows;
- the late stash audit adds 4,008 path rows with zero unresolved semantic
  dispositions, 18 exact source/control adoptions, and six bounded semantic
  ports on later main;
- five stash merge/index/untracked-parent states are independently preserved
  in a verified 27,007,381-byte incremental bundle before live stash
  retirement;
- the validated recovery source commit is
  `bc4c36ff98ebc4cc9b706ba58a10ef559d0497ee`;
- the 65 missing historical tracker events were restored without stale-state
  replay;
- 33,308 hash-listed dirty-root files, 3,679 empty directories, and 28
  separately classified opaque directories were safely retired after
  independent preservation;
- `C:\Comfy_UI_Main_Masking` itself is now the sole registered MaskFactory
  worktree and a clean checkout of local/remote `main`;
- the former dirty state remains reconstructable from an exact remote ref and
  independently readback-verified Tier-A checkpoints;
- coordinated Main cleanup left shared `C:\w` with zero direct children after
  5,654/5,654 checkpoint readback and 84/84 semantic dispositions, after which
  the exact empty parent was removed non-recursively under joint authority; and
- F: remains below the 50 GiB allocation floor, so retained recovery/runtime/
  VHDX authorities remain explicit external authorities and no new large local
  allocation is allowed;
- a fresh full collection has 5,139 nodes: 5,098 passes, 41 governed skips,
  zero failures, and six Pillow deprecation warnings;
- the exact clean export builds, installs without dependencies, and imports;
  its source archive SHA-256 is
  `4d6aa42fb0a2d8c921822c20657995d530c50fa7a4a806cf9b71591931f9ec7f`
  and wheel SHA-256 is
  `644f0aa34a466a11ff607d201d9d4344b994544dbf2b5588f5d82c9e7941c026`;
  and
- full Git integrity passed; exact removal of 13 Git-reported temporary
  objects recovered 93,260,117 bytes and left zero reported garbage; and
- the local pre-commit hook no longer forces generated cache data into the
  repository; its original configuration is preserved in Tier-A.

The verified pre-change rollback authority is:

- checkpoint:
  `C:\MaskFactory_TierA_Backups\second_phase_preconsolidation_20260728T162424Z`;
- registry SHA-256:
  `b2882e7cd50a8bd5f560d85846e6409b754a585b5496dd746aab6cf1ac654197`;
- coverage: all 136 meaningful source/control files, all 110 post-baseline
  files, the exact tracked patch including the additional tracked
  `.codex-ops\CURRENT_TASK.md`, live status/refs/worktrees/origin heads, and the
  existing verified baseline checkpoint; and
- verification: 11 registered artifacts with zero missing, size-mismatched, or
  hash-mismatched entries.

### Required file-to-authority convergence

Every discovered path must receive one row in the durable reconciliation
matrix binding:

1. exact path, owner, repository/common-dir, size, and SHA-256;
2. current and candidate source commit/checkpoint/archive identities;
3. semantic disposition: `ADOPT`, `ALREADY_PRESENT`, `SUPERSEDED`,
   `ARCHIVE_ONLY`, `GENERATED_RUNTIME_ONLY`, `FOREIGN_OWNED`, or
   `BLOCKED_REVIEW_REQUIRED`;
4. governing Plan/spec/Instruction section;
5. affected Item IDs and dependency relationships;
6. affected tracker row IDs, status, verification clause, evidence locator,
   limitations, and completion authority;
7. tests/reconstruction commands and exact result; and
8. final canonical location plus rollback location.

This is not complete when code alone is copied. For every adopted capability,
update the governing Plan, Items, Instructions, tracker rows, dependency DAG,
verification language, evidence locators, and generated reports so they
describe the same implementation and exact commit. For superseded,
archive-only, generated, or foreign-owned bytes, record the disposition and
reason without granting implementation or completion credit.

Tracker state may be changed only through `Plan/Tracker/tracker.py`. Never
hand-edit tracker JSON. Run `validate` and `report`, review the generated
dashboard/phase files, and prove that no orphan, stale dependency, false
completion, missing evidence locator, or unreported adopted capability remains.
Preservation receipts and passing static tests alone never raise a production,
runtime, visual-authority, campaign, or E2E completion tier.

### Terminal conditions for this phase

This phase is complete only when:

1. all 137 original meaningful files, every `refs/stash` worktree/index/
   untracked parent, reflog-only recovery candidate, hook cache, and any later
   arrival have exact semantic and preservation dispositions;
2. every useful capability is present in clean `main` and fully represented in
   Plan, Items, Instructions, tracker/DAG, evidence, and tests;
3. shared `C:\w` has no unowned or unclassified byte and only intentionally
   retained, independently recoverable paths remain;
4. `C:\Comfy_UI_Main_Masking` itself is the clean canonical `main` checkout,
   while the former dirty state remains independently restorable;
5. the standalone Consumer and every original listed directory have a current,
   exact role or receipt-backed removal;
6. local/remote `main`, refs, worktrees, control files, pursuing goal, tracker,
   and coordinating Main task agree on the same final authority;
7. clean install/export, focused and complete applicable suites, tracker
   validation/report, full/connectivity Git fsck, and rollback restoration pass;
   and
8. storage constraints and any retained recovery/VHD/data/evidence roots are
   explicit external authorities, not unreviewed clutter.

## 0B. Repository branch normalization and terminal source recovery: complete

The lossless repository-normalization and `main`-promotion phase completed on
2026-07-28. Its immutable validation/branch plan is
`.codex-ops/NORMALIZATION_VALIDATION_AND_BRANCH_PLAN_20260728T151802Z.json`;
the post-promotion receipt in `.codex-ops` is the final authority for the
result.

Current verified state:

- the integrated product authority was validated at
  `752c0ebfbe97d080ed5ae16fed6f0bd885ff69dd` and promoted to `origin/main`
  through a non-forced fast-forward;
- the fresh 5,139-node complete collection (5,098 passes, 41 governed skips,
  zero failures), changed-file Ruff, compileall, tracker,
  clean-export install/import/focused tests, diff/credential, and
  full/connectivity Git-integrity gates pass;
- remote heads are reduced from 30 to seven: `main` plus six explicit recovery
  refs;
- local branches are reduced from 21 to one: `main`;
- exactly one Masking worktree remains at the physical canonical root;
- five late historical stashes are now independently bundled and retired,
  and the generated pre-commit cache is absent; and
- Consumer, recovery archives, VHDX files, Main-owned paths, and shared
  `C:\w` were not mutated.

The following was the verified topology at the start of this completed phase:

- `origin/main` is `d6a3c0e00d01536504f737aff84891cf8bd4efb3`;
- the clean normalization branch is
  `codex/normalize-maskfactory-main-20260728`; its last pushed checkpoint is
  `23aadb993c0edbe40325ed8417c4e336b7adb600`, 908 commits ahead and zero
  commits behind `origin/main`;
- `origin/main` is an ancestor of the clean reconciliation authority, so the
  eventual update must be a non-forced fast-forward;
- all previously identified product, steward, serverless, historical-agent,
  and dirty-checkout source cohorts are integrated or explicitly preserved and
  dispositioned; the current uncommitted normalization repair set remains to
  pass the final gates and be committed;
- exactly two registered Masking worktrees remain: the protected dirty
  canonical recovery checkout and the clean normalization worktree; and
- the dirty canonical checkout is a checkpoint-protected recovery source, not
  a disposable working tree.

The completed normalization execution record was:

1. finish the active complete collected-suite run and repair any genuine
   failures without weakening external-asset, hash, or authority contracts;
2. prove package/install closure, focused tests, tracker structure, evidence
   reconstruction, secret/credential hygiene, and full Git integrity from a
   clean exported tree;
3. commit and push the exact validated normalization tip, publish a durable
   validation/reconciliation receipt, and exchange the exact commit with the
   coordinating Main task;
4. classify every local and remote branch as required authority, retained
   rollback/recovery authority, merged redundant name, or uniquely preserved
   history; record exact containment before deleting any name;
5. fast-forward `origin/main` to the validated normalization tip
   without force or history rewrite; and
6. retire only branch names proven merged or protected by a durable archival
   ref and disposition receipt, then verify remote `main`, refs, worktrees,
   tracker, clean-export tests, and Git connectivity again.

The terminal conditions applied to this phase were:

1. the complete collected suite passes, with every non-pass explicitly
   classified by a governed external-asset or platform contract;
2. a clean exported checkout installs and passes the required smoke,
   reconstruction, policy, and integrity gates;
3. the normalization worktree is clean and its exact commit is pushed;
4. `origin/main` equals that validated commit after a non-forced fast-forward;
5. every remaining local and remote branch has a durable disposition, and
   only proven-redundant names have been removed;
6. the dirty canonical recovery checkout and required recovery refs/archives
   remain preserved and documented;
7. tracker validation and generated authority reports agree with the promoted
   commit; and
8. the coordinating Main task has received and acknowledged the exact
   promoted commit and the shared-path boundary remains intact.

All eight conditions passed. The subsequent physical-root convergence reduced
the final live topology to one local branch and one worktree; the former dirty
state remains checkpoint/recovery-ref protected without a live dirty checkout.
Repository normalization completion does not mean the entire MaskFactory
product is complete. The portfolio has 53 unresolved hard blockers: 22 are
inside the required core dependency closure and 31 are currently outside all
completion profiles. The core profile has 34 unresolved dependencies total.

Do not mistake fewer branches for correctness. No branch or worktree may be
deleted because of age, naming, or apparent duplication. No `git clean`,
`reset --hard`, bulk garbage collection, bulk `C:\w` deletion, or wholesale
ours/theirs branch merge is permitted. `main` is not current until the
integrated full product—not merely the smaller clean reconciliation line—is
validated and promoted.

## 1. Mission and session relationship

The active MaskFactory session
`019f91d1-ea20-7d81-83ff-03d393eaa1f5` is the authoritative builder of the
Ultimate Masking System. It must finish, integrate, test, qualify, and package
the masking subsystem before that subsystem is adopted by the ComfyUI session
`019f9200-4805-7632-83d3-ee9ae614c603`.

The ComfyUI session is therefore correct to defer its final masking
implementation and acceptance work. It may continue unrelated ComfyUI work,
but it must not independently invent a competing masking implementation,
silently copy unaccepted MaskFactory artifacts, or claim masking completion
before the MaskFactory release/adoption gates pass.

The finished outcome is not merely a collection of scripts, passing unit
tests, a self-hosted model service, or a successful isolated campaign. It is
one coherent, reproducible, end-to-end masking product that:

1. accepts governed image, target, ontology, ownership, side, neighbor, and
   protected-region inputs;
2. generates and repairs provider-neutral mask candidates;
3. enforces deterministic format, ontology, geometry, topology, ownership,
   laterality, protected-region, complete-map, and no-regression checks;
4. uses qualified visual critics only within their measured authority;
5. abstains, rejects, or quarantines when truth or authority is insufficient;
6. stores reconstructable evidence and exact source/runtime identities;
7. serves accepted masks through the MaskFactory runtime and ComfyUI bridge;
8. recovers without duplicate inference, promotion, or unsafe resend; and
9. proves these behaviors in clean, integrated, real end-to-end execution.

## 2. Binding authorities and read order

Read and obey, in order:

1. `Plan/STANDING_ORDERS_AUTONOMOUS_BUILD.md`;
2. `Plan/Tracker/tracker.py` and the live tracker/DAG;
3. `Plan/24_AUTONOMOUS_CORE_COMPLETION_AND_COMFYUI_BRIDGE.md`;
4. `Plan/27_SELF_HOSTED_AUTONOMOUS_LLM_CONTINUOUS_OPERATIONS_SPEC.md`;
5. `Plan/28_ULTIMATE_MASKING_SYSTEM_E2E_INTEGRATION_AND_COMFYUI_ADOPTION.md`;
6. `Plan/Items/23_ITEMS_P6_SELF_HOSTED_AUTONOMOUS_LLM_OPERATIONS.md`;
7. `Plan/Items/24_ITEMS_P6_ULTIMATE_MASKING_SYSTEM_E2E_INTEGRATION.md`;
8. `Plan/Instructions/16_SELF_HOSTED_AUTONOMOUS_LLM_CONTINUOUS_OPERATIONS.md`;
9. `Plan/Instructions/18_ULTIMATE_MASKING_SYSTEM_E2E_INTEGRATION_AND_COMFYUI_ADOPTION.md`;
10. the phase/item source cited by the selected tracker row.

Conversation memory, historical receipts, a dashboard percentage, or a
previously generated packet never overrides the current files and tracker.
The frozen authority registry is:

- `configs/self_hosted_autonomy_contract_freeze_v1.json`;
- `configs/self_hosted_autonomy_campaign_telemetry_v1.schema.json`; and
- `configs/self_hosted_autonomy_acceptance_v1.schema.json`.

Unknown authority fields, duplicate identities, missing required measures,
stale hashes, or incomplete cross-file bindings fail closed.

## 3. Honest current state

The project has made substantial real progress, but the entire product is not
complete.

Accepted and preserved:

- the durable self-hosted steward foundation and restart-safe mission state;
- the real 25-mission engineering campaign (`MF-P6-19.01`) using one owned
  Qwen/vLLM model lifetime, with all 25 missions terminally accounted for;
- the interruption, ambiguity, stale-owner, route-reconciliation, and release
  gate (`MF-P6-19.02`);
- brokered Serverless native-box readiness demonstrated by the accepted V12
  route evidence;
- governed routing, ledger, storage, session-continuity, and no-direct-provider
  controls represented by their accepted tracker evidence; and
- hundreds of focused steward/control-plane tests.

Still incomplete or unproven:

- the governed 100-mask campaign (`MF-P6-19.03`);
- three consecutive target-meeting mixed campaigns and the final throughput
  packet (`MF-P6-19.04`);
- the full 66-class/risk-domain visual qualification gate, including qualified
  primary and independent-family juror authority (`MF-P4-11.23`);
- downstream visual/mask campaign gates `MF-P6-17.03`, `17.04`, `18.01`,
  `18.02`, and `18.04`;
- the original core runtime/ComfyUI bridge rows still open in
  `MF-P6-11.01` through `11.08` and `MF-P6-12.02` through `12.06`;
- acceptance-grade adjudication of the canonical integrated source tree under
  `MF-P6-20.02`; the physical tree, clean diff/secret/package checks, and
  executable recovery exist, but that row has not yet been formally reviewed
  against every dual-source behavior requirement;
- complete baseline classification under `MF-P6-20.03`; the fresh suite
  collected 5,139 nodes with 5,098 passes, 41 governed skips, and zero
  failures, but every skip/external prerequisite and repository-wide lint
  defect still requires explicit disposition;
- a committed, compact evidence locator that makes accepted runtime milestones
  independently discoverable and reconstructable;
- hard-QA semantics that make provider disagreement block, abstain, or route to
  adjudication instead of recording a misleading pass;
- exact runtime/service-health/owned-shutdown reconstruction under
  `MF-P6-20.04`; clean-export build/install/import already passed but does not
  prove live runtime closure; and
- the final pinned MaskFactory release adoption and live masking proof in the
  ComfyUI session.

No phase exit P0 through P9 is currently a fully passing whole-project exit.
No percentage or large test count may be presented as full product completion
while these gates remain open.

## 4. Primary execution strategy

Always select the earliest real dependency-ready parent that advances the
critical path. Prefer executable integration, a real request, an accepted
artifact, and a terminal outcome over another plan, schema, micro-packet, or
bookkeeping-only change.

The current critical paths are:

### Track A — local autonomous masking and product completion

`MF-P4-11.23`
→ `MF-P6-17.03`
→ `MF-P6-17.04`
→ `MF-P6-18.01` / `MF-P6-18.02` / `MF-P6-18.04`
→ `MF-P6-19.03`
→ `MF-P6-19.04`
→ Plan-28 integration and ComfyUI adoption.

Reconcile accepted 25-mission telemetry into `MF-P6-18.03` in parallel without
rerunning `MF-P6-19.01` or `MF-P6-19.02`.

### Track B — governed fallback capacity

When the same real parent cannot use the local GPU, first prove the canonical
actual execution host, deployed manager/config hashes, broker root, and SQLite
ledger through the provider-free preflight. Then, and only then, run one
source-and-parent-bound `decide` → `reserve` → `submit` → `reconcile`
Serverless successor. If the parent phase needs external reasoning, use at most
one sanitized parent-namespaced Qwen-first OpenRouter advisory batch and have
the local controller execute and validate any adoption.

Historical terminal jobs, an idle ledger, detached advisory receipts, fixtures,
or transport-completed semantic failures are not fallback-capacity credit.

### Track C — whole-product convergence

In parallel CPU-safe lanes:

- reconcile the accepted autonomy/runtime commits into the canonical full
  product source tree without losing either side;
- restore a reproducible package/install definition and exact runtime closure;
- classify and repair the full-suite failures;
- build the evidence locator and clean reconstruction procedure;
- correct disagreement and abstention semantics; and
- keep the ComfyUI handoff contract current without adopting the masking
  release prematurely.

## 5. Self-hosted autonomous system requirements

The CPU-safe supervisor and durable campaign ledger must continuously:

- read the pursuing goal, tracker, dependency DAG, active work, immutable
  receipts, route state, and authority ceilings;
- select useful unblocked work and persist intent before execution;
- reject completed, duplicate, active, superseded, or ambiguous work;
- batch compatible work rather than creating repeated micro-requests;
- reconstruct after process, service, session, or host interruption;
- continue CPU-safe work when inference is unavailable;
- emit one consolidated campaign packet rather than routine intermediate
  handoffs; and
- never infer completion from conversation state.

Local GPU work must use `tools/run_with_shared_pod_gpu_lease.py`, the
authoritative FIFO database
`/workspace/.maskfactory/shared_pod_coordination/shared_gpu_leases_v1.sqlite`,
and the protected MaskFactory owner token. No lease means no local GPU launch.
Never kill or preempt another session's process. Release the GPU after each
atomic work cell.

Serverless is broker-only. OpenRouter is governed Qwen-first, read-only
advisory reasoning only. Never call providers or endpoints directly, never
dual-submit, never use a second paid Pod, and never substitute Claude, Cursor,
or EC2.

The self-hosted worker may prepare bounded patches, focused tests, diagnosis,
repair proposals, evidence, and tracker-ready recommendations. It does not
receive credentials, Git/GitHub final authority, destructive filesystem,
infrastructure, RunPod lifecycle, final tracker, final mask, or final adoption
authority.

## 6. Mask-generation and visual-authority requirements

Every mask record must bind exact source, label, owner, side, neighbor,
protected-region, ontology, transform, provider, and parent identities.
Missing or ambiguous semantics must abstain before inference or defect
generation.

Every candidate must pass deterministic hard QA before visual consideration.
Hard QA always vetoes visual confidence. Provider disagreement is not a pass:
it must block, abstain, quarantine, or enter explicitly governed adjudication.

Visual approval requires:

- a source-bound full 66-class/risk-domain regression suite;
- direct visual confirmation rather than metadata-only hints;
- a calibrated high-end primary visual critic;
- a calibrated independent-family juror;
- exact positive/negative controls and authority ceilings;
- disagreement, unavailable, malformed, timeout, and hard-QA-override tests;
  and
- immutable source/mask/overlay/contour/ownership evidence.

Text-only reasoning cannot approve masks. Synthetic or static controls may
test the control plane but cannot qualify real-image visual authority.

## 7. Canonical product integration requirements

The accepted autonomy work and the full MaskFactory product must converge into
one canonical build. A smaller active branch passing steward tests does not
prove that the full product works, and a larger product branch without the
accepted runtime does not prove autonomy.

Before whole-product completion:

1. inventory both source lines by commit and path;
2. classify every unique source, test, config, runtime, schema, and evidence
   path;
3. reconcile conflicts without reset, loss of unrelated dirty work, or a
   replacement worktree used to evade ownership;
4. ensure the integrated tree contains `pyproject.toml`, install/runtime
   definitions, product code, autonomy code, tests, schemas, and operating
   procedures;
5. run focused suites and then the full product suite from a clean environment;
6. classify external-asset tests as provisioned, hermeticized, or explicitly
   governed—not silently skipped;
7. prove exact-byte runtime and evidence reconstruction; and
8. preserve accepted campaign identities without rerun.

Static tests are necessary but not sufficient. A tracker row becomes complete
only when its verify clause has direct evidence.

## 8. Evidence and completion discipline

Every accepted milestone must have one discoverable evidence-index entry
binding:

- tracker item and parent campaign;
- source commit and dirty-path boundary;
- input, request, model, runtime, config, response, artifact, validation,
  acceptance, terminal, and release hashes as applicable;
- exact local/Pod location and compact recovery location;
- replay/reconstruction command;
- limitations and authority not claimed; and
- adoption disposition.

Missing local copies of historical tracker-cited receipts are not current
authority merely because their Git objects still exist. Recover historical
provenance as history, then generate a new source-bound qualified receipt when
current authority is required.

Do not advance runtime rows from commits, schemas, manifests, or fixture tests
alone. Do not advance visual rows from labels, filenames, metadata, or contact
sheets alone. Do not advance product integration from a partial suite.

## 9. ComfyUI adoption boundary

MaskFactory must publish an immutable release candidate only after its required
core, throughput, integration, reconstruction, and visual/mask gates pass.
The handoff must bind:

- MaskFactory release commit/tree and package/runtime hashes;
- supported ontology, labels, modes, providers, and authority limitations;
- `/predict` and artifact contracts;
- health, startup, shutdown, rollback, and invalidation procedures;
- focused and full-suite results;
- real masking campaign evidence;
- exact ComfyUI integration inputs and expected outputs; and
- a cross-session adoption receipt.

The ComfyUI session then independently validates and adopts that exact release.
It must prove at least one live masking workflow for each required serving mode,
including failure, abstention, rollback, and restart behavior. No unpinned
copy, model output, or advisory proposal may cross the boundary as accepted
code or mask truth.

## 10. Anti-spin and coordination rules

- Preserve `MF-P6-19.01` and `MF-P6-19.02`; never rerun them.
- One immutable parent may have one active route and one terminal disposition.
- Prefer one campaign-sized request over repeated micro-requests.
- Bookkeeping/hygiene is at most 10% of effort unless it repairs a blocking
  defect.
- Do not build successor packets without a material immutable contract change.
- Do not create a replacement clone/worktree to avoid the shared checkout.
- Preserve unrelated staged, dirty, and untracked user work.
- Coordinate path ownership before editing shared authority or active runtime
  files.
- Reuse only the two pinned project heartbeat threads; no standalone session-
  spawning cron loops.
- Report production deltas: executable integration, real requests, accepted
  artifacts, terminal outcomes, and blocker changes.

## 11. Completion claims

The following claims remain distinct:

- `SELF_HOSTED_AUTONOMOUS_LLM_THROUGHPUT_INCOMPLETE` remains binding until
  `MF-P6-19.03` and `MF-P6-19.04` pass.
- `ULTIMATE_MASKING_SYSTEM_E2E_INCOMPLETE` remains binding until the required
  core rows, Plan-27 throughput gate, Plan-28 integration items, clean full
  suite, evidence reconstruction, and ComfyUI adoption all pass.
- Optional independent-real-accuracy and scale/DAZ claims remain separate and
  cannot be silently imported into or omitted from the required core claim.

The entire project is complete only when:

1. every required `core_autonomous_runtime` item and dependency is complete;
2. the 25-mission, recovery, 100-mask, and three mixed campaigns pass;
3. the visual primary/juror authority is qualified;
4. the canonical integrated tree installs and passes focused plus full suites;
5. accepted evidence is independently reconstructable;
6. hard-QA disagreement and abstention semantics fail closed;
7. the immutable release is adopted by the pinned ComfyUI session;
8. live ComfyUI masking E2E and rollback/restart pass; and
9. the tracker, generated reports, release packet, and both sessions agree on
   the same exact completion state.

Until then, continue the earliest dependency-ready real lane and report the
remaining blocker honestly.

# Phase P5: Custom Model Training -- Live Status

**AUTO-GENERATED by `tracker.py report` from tracker.json -- do not hand-edit.**
Source checklist: `Plan/Items/06_ITEMS_P5_TRAINING.md` | Entry gate: metrics.approved_gold_count >= 200

**Progress: 18/38 (47.4%)** -- blocked 0 | in progress 11 | failed 0 | deferred 0 | open 9


### MF-P5-01 — Dataset build v1 + DVC (spec: 12 §1/§3)

- ☑ **MF-P5-01.01** -- `datasets\splits.py`: bucket = int(sha256(image_id)[:8],16) % 100 → train 0–69 / val 70–84 / test 85–99 · pHash near-dup guard (Hamming ≤ 6 → follow earlier image's split) · synthetic → train-only enforced · `datasets\hard_case_holdout.txt` override list honored, never trains/tunes
    - Evidence: assign_splits uses exact sha256(image_id) bucket thresholds, pHash Hamming<=6 connected groups, generated/synthetic train-only groups, and hard-case override precedence; multi-instance split integrity validator and seeded tests pass.
- ☑ **MF-P5-01.02** -- `datasets\builder.py`: full layout — dataset_card.md, train/val id lists, part_seg\, material_seg\, hand_crops\, matting\, projected\, coco\annotations.json (RLE), holdout\ (read-only export)
    - Evidence: build_dataset exports the full specified layout and now routes every COCO annotation through strict datasets/cocorle.py uncompressed RLE: canonical Fortran-order runs, required initial background count, exact size/count coverage, strict binary source validation, decoder and corruption rejection. Non-square/edge/random round trips plus S14 integration prove decoded geometry and area match annotations; 20 focused and 554 full tests pass.
- ☑ **MF-P5-01.03** -- Pre-flight: `verify-package` on every eligible package · any hash/format mismatch hard-fails the build
    - Evidence: Builder calls verify_packages on every frozen eligible gold before creating destination; seeded invalid verification aborts with no bodyparts@v1 directory.
- ☑ **MF-P5-01.04** -- Byte-identical rebuild test (sorted inputs, seed 1337, zeroed zip timestamps) — G8 for datasets
    - Evidence: Dual build roots from identical sorted inputs produce identical relative-file SHA256 maps, including coverage/card/manifests; deterministic timestamp is fixed and seed 1337 recorded.
- ☑ **MF-P5-01.05** -- Holdout isolation: trainers receive no read path to holdout\ · enforcement test proves it
    - Evidence: Holdout instances are written only under holdout/test or holdout/hard_case, absent from trainer trees/lists; build manifest sets holdout_trainer_read_path=null and integrity fixture proves isolation.
- ☐ **MF-P5-01.06** -- Build `datasets\bodyparts@v1` · `dvc add` · `git tag dataset/bodyparts-v1` · `dvc push` to s3://maskfactory-dvc-dev
- ☑ **MF-P5-01.07** -- Dataset card audit: counts per split/class/cell · ontology version · build cmd + git sha · synthetic ratio recorded
    - Evidence: Dataset card records split image/instance counts, every visible class count, all view/pose/solo-duo-small_group cells, ontology version, exact build command, Git SHA, seed and synthetic ratio; audit assertions pass.
- 🟨 **MF-P5-02.01** -- 99% -- MMSeg custom dataset class + transform stack · ignore_index 255 (ambiguous_do_not_use zones burned to 255)
    - Evidence: qa/live_verification/bodypart_class_contract_20260712.json proves the doc-18 resolution, corrected configs, direct compiler acceptance, 57-drift refusal before staging, 21 focused and 639 full tests. Actual CUDA BaseSegDataset execution remains open.
    - Note (2026-07-11T17:24:11+00:00, ai_agent): Pinned the exact compatible OpenMMLab stack in env/openmmlab_training_stack.lock.json (MMEngine 0.10.7, full MMCV 2.1.0 source commit, MMSeg 1.2.2, MMDetection 3.3.0), added maskfactory training-doctor, and made optional MMSeg loading fail closed on broken transitive installs. A real isolated MMSeg/mmcv-lite probe now exposes missing mmcv._ext instead of silently disabling dataset registration. The actual full-MMCV CUDA BaseSegDataset run remains pending the Kevin-session WSL identity boundary and the unresolved 56-vs-57 class contract. 34 focused and 481 full tests pass; Ruff clean.
    - Note (2026-07-11T17:41:53+00:00, ai_agent): Completed the actual MMSeg transform bridge: four registered wrappers consume img/gt_seg_map, preserve geometry, force 40% rare-class crops (all fingers/toes/belly_button or material strap), remap every sided ID on p=0.5 flip, apply BGR-correct image-only jitter, and rotate labels nearest with border 255. mmseg_augmentation_config strictly compiles the governed YAML order/parameters; mmseg_training_dataset_bundle makes dataset custom imports and pipeline inseparable; training-doctor now requires all transforms registered. 20 focused tests pass; seeded QC retry 12/12; full regression 487 passed; Ruff clean. Final 1% remains a real full-MMCV CUDA BaseSegDataset execution in Kevin's WSL runtime.
    - Note (2026-07-12T10:58:59+00:00, ai_agent): Resolved the obsolete 56/57 blocker from approved doc 18: active v1 IDs 0..55 include background 0 and therefore require 56 logits; v2 IDs 0..64 require 65. Both governed v1 configs now match the adapter's 56-class vocabulary and compile directly. Final 1% is only the real pinned full-MMCV CUDA BaseSegDataset execution in Kevin's WSL runtime; training-doctor confirms this Windows process lacks that stack.
- ☑ **MF-P5-02.02** **[HARD BLOCKER]** -- Horizontal flip p=0.5 WITH swap_partner ID remap for all sided PART + MATERIAL labels · fixture-asserted unit test wired as a CI BLOCKER for all training merges
    - Evidence: src/maskfactory/training/augmentations.py derives reciprocal uint8 remaps exclusively from configs/ontology.yaml through Ontology; flips image width and remaps all sided PART/MATERIAL IDs, preserves 255, rejects unknown IDs. tests/test_training_augmentations.py passes 5 tests covering every current ontology ID/all 42 sided part IDs, involution, p=0.5 boundary, paired maps, and hard failure. .github/workflows/ci.yml has dedicated blocking pytest step; YAML validated.
- ☑ **MF-P5-02.03** -- RandomResizedCrop → 512², scale 0.5–2.0 · 40% of crops forced to contain a rare-class pixel (fingers/toes/belly_button/straps) · sampling rate measured and logged
    - Evidence: random_resized_crop emits configured 512 targets with scale 0.5-2.0, selects rare-class pixels on probability 0.4, explicitly preserves forced subpixel rare labels, and RareSamplingMeter atomically logs attempts/forced/contained/rate; seeded 200-trial rate is within tolerance.
- ☑ **MF-P5-02.04** -- Photometric jitter (b/c/s ±0.25, hue ±0.05; labels untouched) · rotation ±15° nearest with border → 255
    - Evidence: photometric_jitter enforces brightness/contrast/saturation +/-0.25 and hue +/-0.05 without label input; rotate_sample enforces +/-15, bilinear RGB, nearest labels and border 255; fixture proves labels unchanged by jitter and valid rotated vocabulary.
- ☑ **MF-P5-02.05** -- Banned-aug guard: configs reject vflip / elastic / perspective / MixUp / CutMix
    - Evidence: validate_augmentation_config recursively rejects vflip/verticalflip, elastic, perspective, MixUp and CutMix anywhere in nested configs; each banned transform has a failing fixture while horizontal_flip passes.

### MF-P5-03 — Train 6.1 body-part segmenter (spec: 12 §5/§6.1)

- ☑ **MF-P5-03.01** -- Author `configs\training\bodypart_segformer_b3.yaml`: 56-class v1 (IDs 0..55 including background; v2 is 65-class per doc 18) · 512 crops · AdamW 6e-5 poly 1.0 · 40k iters (80k @500 gold) · warmup 1.5k · CE+Dice · class weights ∝ 1/√pixel_freq cap ×8 · bf16 AMP · batch 2 × grad-accum 8
    - Evidence: configs/training/bodypart_segformer_b3.yaml now encodes the authoritative 56-class v1 contract plus all original schedule/optimizer/loss/AMP requirements. qa/live_verification/bodypart_class_contract_20260712.json records doc-18 authority, compile acceptance, deliberate 57-drift refusal, and 639 full tests.
    - Note (2026-07-11T09:21:21+00:00, ai_agent): Execution audit found a spec conflict: this completed YAML correctly encodes the literal requested num_classes=57, but authoritative part IDs 0..55 already include background, yielding only 56 indexed targets. No training may be credited until NEEDS KEVIN decision in DECISIONS_LOG resolves whether YAML becomes 56 or ontology gains a real ID 56.
    - Note (2026-07-12T10:59:00+00:00, ai_agent): Supersedes the historical unresolved note. Approved doc 18 explicitly invalidates 57 classes: active v1 is 56 logits including background ID 0; append-only v2 will be 65. The SegFormer-B3 config is corrected to 56 and the production compiler accepts it without test-time mutation.
- 🟨 **MF-P5-03.02** -- 75% -- Train SegFormer-B3 under `runs\gpu.lock` · eval per-part IoU + boundary-F on val every 4k · thermal cooldown (sleep 60 s @ >87 °C) verified during a long run
    - Note (2026-07-11T10:09:46+00:00, ai_agent): Implemented mandatory in-process MMEngine thermal hook: exact 30-minute poll, hottest-GPU nvidia-smi probe, >87C triggers 60-second trainer-thread sleep, fsynced thermal.jsonl evidence, and strict governed config bridge. Boundary tests prove 87.0 no-cool and 87.0001 cool; live probe returned 42C. 22 thermal/config/run/GPU-lock tests pass; Ruff/format/diff clean. Actual SegFormer training remains gated by no eligible dataset, absent MMSeg locks/runtime, and unresolved 56-vs-57 class count.
    - Note (2026-07-11T17:59:28+00:00, ai_agent): Activated the real SegFormer-B3 training boundary: governed YAML compiles to self-contained MMEngine/MMSeg config with official B3 architecture/pretrain, train/val dataset bundles, inverse-sqrt pixel-frequency CE weights capped x8, CE+Dice, bf16 AmpOptimWrapper, batch2 x accumulation8, 1500 warmup/poly1, deterministic seed, 4k validation/checkpoint cadence, registered per-class IoU + boundary-F@2px metric, thermal hook, >=200-instance entry gate, runtime doctor, and exclusive runs/gpu.lock. maskfactory train now executes MMEngine, captures logs/command/config, and refuses zero-exit without checkpoint. Real CUDA long run/thermal event/eval rows remain. 33 focused and 501 full tests pass; Ruff clean.
    - Note (2026-07-12T10:59:01+00:00, ai_agent): Class-count blocker removed: active SegFormer training config now compiles at the authoritative v1 56 logits. Remaining gates are unchanged and real: >=200 approved gold, pinned full-MMCV CUDA runtime in Kevin's WSL identity, long thermal run, holdout evaluation, and champion decision.
- 🟨 **MF-P5-03.03** -- 65% -- Challenger: Mask2Former-SwinB config + run (activation checkpointing) — optional Swin-L only via AWS burst (05-08.03)
    - Evidence: qa/live_verification/bodypart_class_contract_20260712.json proves corrected SegFormer and Mask2Former configs plus drift refusal. Mask2Former compiler structure remains focused-test covered; no run/checkpoint is claimed.
    - Note (2026-07-11T17:24:13+00:00, ai_agent): Authored bodypart_mask2former_swinb.yaml with local Swin-B, activation checkpointing in backbone/trainer, 512 crops, bf16, effective batch 16, native matcher CE/BCE/Dice losses, 40k/80k schedule, 4k evaluation, hard holdouts, exact thermal hook policy, and Swin-L restricted to MF-P5-08.03 AWS burst. Class-contract preflight intentionally refuses both body-part configs while the literal 57 logits conflict with authoritative IDs 0..55; no challenger run is claimed. 34 focused and 481 full tests pass; Ruff clean.
    - Note (2026-07-11T17:59:29+00:00, ai_agent): Mask2Former-SwinB governed config now compiles into the pinned official Swin-B/Mask2Former structure with with_cp activation checkpointing, mmdet pixel decoder/Hungarian matcher/native classification-mask-dice losses, class-frequency weights, bf16 accumulation, Swin-specific paramwise optimizer, 4k IoU/BF validation, thermal hook, and the same fail-closed launcher/evidence contract. Swin-L remains AWS-only. No challenger run or checkpoint is claimed; >=200 gold, full CUDA runtime, and 56/57 decision remain. 33 focused and 501 full tests pass; Ruff clean.
    - Note (2026-07-12T10:59:01+00:00, ai_agent): Class-count blocker removed from the challenger: Mask2Former-SwinB now declares and compiles 56 v1 classes; its matcher retains the framework-required extra no-object classification weight. Remaining work is the actual >=200-gold CUDA challenger run/evaluation; Swin-L remains AWS-only.
- ☐ **MF-P5-03.04** -- Final eval on frozen test_holdout + hard_case_holdout → leaderboard rows with full per-class + group scores
- ☑ **MF-P5-03.05** -- Every run logged to `runs\<run_id>\` {run.json, config, git_sha, dataset_ref + DVC md5, ckpts, tb, eval}
    - Evidence: Transactional training-run initializer creates exact required run.json/config.yaml/git_sha/dataset_ref/dataset_dvc_md5 plus ckpts/tb/eval via staging+atomic rename; requires bodyparts@vN and 32-hex DVC identity, freezes config SHA, refuses collisions, and enforces initialized->running->complete|failed with immutable final states. CLI exposes explicit --initialize-only and refuses fake training. 14 focused tests pass; Ruff/format/diff clean.

### MF-P5-04 — Train 6.2 clothing/material parser (spec: 12 §6.2)

- ☑ **MF-P5-04.01** -- Author `configs\training\clothing_segformer_b2.yaml`: 16-class · thin classes (strap/waistband/lace) ×4 crop weight · 30k iters
    - Evidence: clothing_segformer_b2.yaml encodes 16 classes, 512 crops, 30k iters, bf16/effective batch 16, x4 strap/waistband/lace sampling, and SCHP/S08 plus 0.55 thin-class promotion gates; config test passes.
- 🟨 **MF-P5-04.02** -- 70% -- Train + eval · GATE: beats SCHP+S08 heuristics on material mIoU AND strap/waistband IoU ≥ 0.55
    - Note (2026-07-11T09:49:29+00:00, ai_agent): Implemented fail-closed clothing promotion evaluator against exact schp_atr_plus_s08_heuristics baseline on identical frozen test_holdout: candidate material mIoU must be strictly higher and strap/waistband IoU each >=0.55. Atomic evidence writer and invalid/missing/noncomparable refusal included. 27 focused clothing-gate/S08/config tests pass; Ruff format/check and diff check clean. Real 30k training and measured holdout rows await eligible gold dataset.
- 🟨 **MF-P5-04.03** -- 85% -- On win: promote to S08 primary (SCHP demoted to fallback) via registry role edit
    - Note (2026-07-11T09:46:13+00:00, ai_agent): Implemented promotion-aware S08 primary: absent champion role leaves existing SCHP/Sapiens/GDINO/SAM2 path unchanged; exactly one verified champion_clothing plus loader supplies native 16-class map with strict IDs/geometry/silhouette coverage, checkpoint provenance, and provider close. Evidence names schp_plus_s08_heuristics fallback; tampered weights hard-fail. 24 focused tests pass; Ruff/diff clean. Live promotion/fallback awaits MF-P5-04.02 winner.
- ☑ **MF-P5-05.01** -- Author `configs\training\hand_segformer_b2.yaml`: 14-class (bg, hand_base L/R, 10 fingers, finger_occlusion_boundary) · 768 window on 1024 crops · multi-scale 0.75–1.25 · 25k iters · finger swap_partner remap in flips
    - Evidence: hand_segformer_b2.yaml encodes exact 14-class vocabulary, 1024 sources/768 windows, 0.75-1.25 scale, 25k iters, finger-required crops, swap_partner flips, bf16 and D7/false-split/paste-back gates; config test passes.
- ☑ **MF-P5-05.02** -- Build the seeded ambiguous-hand audit set (merged-finger cases with known truth) for the false-split metric
    - Evidence: qa/hand_audit contains deterministic seed-1337 manifest plus 100 RGB cases, 100 indexed known-truth hand-base maps, and 100 strict binary ambiguity masks (50 left/50 right). Evaluator defines false split as any affected finger-class pixel in ambiguity region and proves strict boundary: 0/1 cases pass, 2/100=2% fails. Full rebuild byte-identical; missing/unknown predictions refuse. 14 focused tests pass; Ruff/format/diff clean.
- ☐ **MF-P5-05.03** -- Train + eval on the hand-crop holdout
- 🟨 **MF-P5-05.04** -- 70% **[HARD BLOCKER]** -- GATE (**D7**): finger-class mean IoU ≥ 0.70 AND merged-finger false-split rate < 2%
    - Note (2026-07-11T09:40:21+00:00, ai_agent): Implemented indivisible D7 evaluator requiring frozen test_holdout fingers IoU >=0.70, >=100-case merged-finger false-split rate strictly <0.02, and paste-back IoU >=0.995. Exact pass/fail boundaries, incomplete evidence refusal, and atomic result writer tested; 19 focused tests pass, Ruff/diff clean. Hard blocker remains: no trained hand model or real holdout row exists.
- 🟨 **MF-P5-05.05** -- 85% -- On win: replace lane steps 2.3–2.4 as crop drafter (SAM2 stays the interactive editor) · model outputs pass QC-018 paste-back ≥ 0.995
    - Note (2026-07-11T09:43:31+00:00, ai_agent): Implemented verified champion_hand crop drafter: resolves exactly one hash-matching role, validates native integer 14-class output and side ownership, converts to HandGeometry (five fingers/base/gaps), then closes provider. SAM2 is not part of drafting and remains separate refine_hand_with_sam2 API; existing QC-018 evaluator enforces paste-back >=0.995. 26 focused tests pass; Ruff/diff clean. Live activation awaits a D7-winning checkpoint.

### MF-P5-06 — Leaderboard + promotion mechanics (spec: 12 §10)

- 🟨 **MF-P5-06.01** -- 88% -- `training\leaderboard.py` full schema writer · standing baselines auto-scored per dataset version: sam2_only · sam2_pose · sam2_parsing · draft_pipeline_full
    - Note (2026-07-11T07:47:09+00:00, ai_agent): Full validated leaderboard row schema, durable JSONL writer, reader, and context breakout are implemented. Standing-baseline auto-scoring per dataset version remains open.
    - Note (2026-07-11T08:47:17+00:00, ai_agent): Added ensure_standing_baselines orchestration: exact required four baselines, stable dataset_ref/split identity stamping, schema validation, missing-only scoring, idempotent reruns, and automatic fresh scoring requirement for new dataset versions. 16 focused leaderboard/hand/IAA tests pass; Ruff and diff checks clean. Actual measured baseline rows remain gated by real holdout data.
    - Note (2026-07-11T09:26:11+00:00, ai_agent): Leaderboard run_id is now immutable: exact normalized rerun is no-write idempotent; changed payload under same ID hard-fails before mutation; comparisons reject duplicate IDs; standing baseline orchestration rejects multiple rows for one family/dataset/split. 18 leaderboard/hand/IAA tests pass; Ruff/diff clean. Actual baseline scores remain gated by approved holdout data.
- ☑ **MF-P5-06.02** -- `maskfactory leaderboard --compare <a> <b>` per-class delta table (group rows: fingers, toes, chest_boundary, hairline, bands)
    - Evidence: maskfactory leaderboard --compare defaults to a stable Markdown table with pooled, per-class, fingers/toes/chest_boundary/hairline/bands group, and instance-context IoU/BF deltas; JSON remains available; comparison tests pass.
    - Note (2026-07-11T07:47:09+00:00, ai_agent): CLI --compare now emits pooled, per-class, group, and instance-context deltas with dataset/split comparability enforcement. Human-oriented delta table rendering remains open; current output is deterministic JSON.
- 🟨 **MF-P5-06.03** -- 98% -- Champion pointers in `models\model_registry.json` (`role: champion_bodypart` etc.) · all loaders/serving read champions ONLY
    - Note (2026-07-11T19:27:03+00:00, ai_agent): Serving now resolves champion roles only and will not configure /predict for a partial champion set; each checkpoint/config/vocabulary is verified before framework load. S09 already requires champion_bodypart provenance. Real champion pointers remain gated by trained winners and promotion evidence.
    - Note (2026-07-11T19:34:56+00:00, ai_agent): Promotion into champion_bodypart/champion_hand/champion_clothing now re-resolves and hash-checks the candidate checkpoint, independently verifies a safe in-models_root inference config and SHA-256, and rejects empty/duplicate/unknown/wrong-map class vocabularies before any registry mutation. Tampered-config and part-vs-material fixtures prove the incumbent role remains unchanged on refusal. 10 focused registry and 534 full tests pass. Remaining evidence is an actually trained/evaluated winner registered with these artifacts, promoted, served, and rolled back.
    - Note (2026-07-11T19:46:47+00:00, ai_agent): Completed the producer side of champion provenance. Successful MMSeg training now seals candidate_artifact.json with exact run/dataset identity, checkpoint/config SHA-256, target champion role, and class vocabulary before the run becomes complete. maskfactory models register-training-candidate accepts only that matching completed run, copies both artifacts atomically into models/trained/<key>, re-hashes copies, registers only as challenger_bodypart, and rolls back installed files if registry mutation fails. Tamper fixture proves no durable state appears. Shared bounded PermissionError retry now hardens atomic directory promotion on Windows without retrying semantic errors. 536 full tests pass; Ruff/format/ontology clean. Remaining 2% is a real evaluated winner registration, promotion, live serve, and rollback.
- ☑ **MF-P5-06.04** -- Demonstrate promotion AND instant rollback — one registry edit each way
    - Evidence: Fixture promotes verified challenger into champion_bodypart by one atomic registry role swap, resolves it by role, records incumbent/history, then rollback_model_role performs one atomic inverse edit restoring both exact original roles.
- ☑ **MF-P5-06.05** -- Human-ceiling row from IAA plotted · saturation rule noted (within 2 pts of human → stop chasing, mine elsewhere)
    - Evidence: Weekly IAA writer now emits a schema-valid human_ceiling_iaa leaderboard input with per-class/group IoU+BF and zero-checkpoint convention; saturation_report marks classes within 0.02 on both metrics; second-review and saturation tests pass.

### MF-P5-07 — Champion into the pipeline (spec: 12 §6.1/§7)

- 🟨 **MF-P5-07.01** -- 85% -- S03/S09 consume `custom_bodypart` as a consensus source at weight 0.45 (fusion-weights update path in pipeline.yaml)
    - Note (2026-07-11T09:30:46+00:00, ai_agent): Implemented champion-ready S09 integration at custom_bodypart=0.45. Indexed S03 custom map is accepted only with sidecar role+checkpoint SHA matching the verified champion_bodypart registry role; invalid/missing provenance and IDs hard-fail. Consensus records the source. Expanded profile without custom artifact is byte-identical to base fusion. 28 focused tests pass; Ruff/diff clean. Live S03 inference/promotion remains gated by training/D6.
- 🟨 **MF-P5-07.02** -- 70% **[HARD BLOCKER]** -- Verify **D6/G7**: champion beats draft_pipeline_full on frozen test_holdout in BOTH mean per-part IoU and boundary-F, with NO tracked hard class regressing > 2 pts
    - Note (2026-07-11T09:54:45+00:00, ai_agent): Implemented fail-closed D6/G7 evaluator against exact draft_pipeline_full baseline on identical frozen test_holdout. Candidate must strictly beat pooled mean IoU and boundary-F, with fingers/toes/chest_boundary/hairline IoU and BF each no worse than -0.02. Atomic evidence writer and invalid/incomplete/noncomparable refusal included. 38 focused body-part-gate/leaderboard/S09 tests pass; Ruff format/check and diff check clean. Real trained champion and measured holdout rows remain required.
- ☐ **MF-P5-07.03** -- Remeasure G1 (target trend ≤ 12 min/img) + publish G2/G3 numbers
- ☐ **MF-P5-07.04** -- QC-034 regression sweep on a re-processed sample after the swap — clean

### MF-P5-08 — Conditional / optional models (spec: 12 §6.4–6.5/§5)

- ☐ **MF-P5-08.01** _(conditional)_ -- (Trigger: ≥ 80 hair-prominent golds) ViTMatte fine-tune · GATE: hair boundary-F ≥ 0.65 AND matte MSE −15% vs stock
- ☐ **MF-P5-08.02** _(conditional)_ -- (Trigger: ≥ 120 approved projected labels AND chest-lane fail rate > 10%) breastproj SegFormer-B1 · GATE: projected IoU ≥ 0.75 · outputs provenance-tagged `model:breastproj@<run>`, purple-editable, never truth
- ☐ **MF-P5-08.03** -- (Optional) AWS burst runbook executed once: g6e.xlarge L40S spot in dev 548846591581 · git clone + `dvc pull` · train · `dvc push` + runs sync · instance terminated with the run

### Phase Exit Gate

- ☐ **MF-P5-EXIT** -- Custom models are the drafters · leaderboard is the arbiter · **D6 + D7** checked with evidence rows · doc 14 §6 checkboxes updated
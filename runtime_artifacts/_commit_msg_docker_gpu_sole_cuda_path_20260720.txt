docker-gpu(cuda): declare Docker-GPU sole CUDA train/serve path; defer WSL repair

WSL Ubuntu-22.04 repair is blocked (non-admin shell cannot run the scripted
elevated e2fsck; on-disk ext4 VHD corrupt, /bin/true -> Error code 6/E_FAIL) and
is DEFERRED - it only gates the WSL-specific live SAM 3.1 CUDA smoke (MF-P2-11.07).
Docker-GPU is doubled-down as the sole/primary local CUDA train+serve runtime.

Evidence (this wave):
- GPU passthrough live-proven: RTX 5060, driver 592.01, 8151 MiB via --gpus all.
- serve+train Docker build paths STATIC_PASS and WSL-Ubuntu-distro-INDEPENDENT
  (Dockerfile.serve python:3.11-slim+torch cu128; Dockerfile.train
  nvidia/cuda:12.8.0-devel Docker Hub base). F: (removable) not required either.
- Heavy image builds deliberately deferred out-of-band per DOCKER_RUNTIME sec 6b
  build-safety (protect the running production CVAT stack); build-path viability
  established, not build success.

No tier inflation: champions=0, gold=0, no torch-CUDA-in-container, no
containerized serve/train green, no doctor-all-green. Seal:
docker_gpu_sole_cuda_path_wsl_deferred_20260720.json (self_sha256 ca82795a...).

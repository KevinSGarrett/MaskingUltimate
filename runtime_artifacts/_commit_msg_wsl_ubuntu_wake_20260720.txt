evidence(wsl): Ubuntu-22.04 wake DONE (Error code 6 -> wsl --shutdown -> ok); nvidia-smi PASS; torch env next

Live-probed this wave: wsl -d Ubuntu-22.04 hit Error code 6 (E_FAIL, matching the
previously documented ext4 corruption signature); `wsl --shutdown` (non-destructive
full WSL2 VM restart) brought it back to Running/responsive. nvidia-smi inside the
distro passed (RTX 5060 Laptop GPU, driver 592.01, 8151 MiB). System python3
(3.10.12) has no torch installed (ModuleNotFoundError) -> next agent step is a
venv + pip install torch in-distro before the live SAM 3.1 CUDA WSL smoke.

Sealed qa/live_verification/wsl_ubuntu_wake_20260720.json and updated
needs_agent_actions_20260720.json (repair_ubuntu_2204_ext4_vhd ->
WSL_WAKE_DONE_TORCH_ENV_NEXT). Root cause (on-disk ext4 corruption) is not
claimed repaired (no e2fsck run); Docker-GPU remains the durable fallback CUDA
path regardless. No tier inflation.

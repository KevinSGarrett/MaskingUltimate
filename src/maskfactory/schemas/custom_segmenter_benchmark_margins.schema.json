{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://maskfactory.local/schemas/custom_segmenter_benchmark_margins.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "manifest_id",
    "frozen_at",
    "authority",
    "results_state",
    "delta_convention",
    "source_hashes",
    "role",
    "sha256"
  ],
  "properties": {
    "schema_version": {"const": "1.0.0"},
    "manifest_id": {"const": "custom_segmenter_noninferiority_v1"},
    "frozen_at": {"type": "string", "format": "date-time"},
    "authority": {"const": "predeclared_thresholds_only_no_result_or_promotion_authority"},
    "results_state": {"const": "sealed_before_results"},
    "delta_convention": {"const": "positive_is_challenger_improvement"},
    "source_hashes": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {"$ref": "#/$defs/sha256"}
    },
    "role": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "target_provider_role",
        "primary_objective",
        "labor_objective",
        "hard_labels",
        "high_risk_contexts",
        "label_margins",
        "context_margins",
        "zero_regression_metrics"
      ],
      "properties": {
        "target_provider_role": {"const": "custom_segmenter"},
        "primary_objective": {"$ref": "#/$defs/objective"},
        "labor_objective": {"$ref": "#/$defs/objective"},
        "hard_labels": {"$ref": "#/$defs/nameArray"},
        "high_risk_contexts": {"$ref": "#/$defs/nameArray"},
        "label_margins": {"$ref": "#/$defs/marginMap"},
        "context_margins": {"$ref": "#/$defs/marginMap"},
        "zero_regression_metrics": {"$ref": "#/$defs/nameArray"}
      }
    },
    "sha256": {"$ref": "#/$defs/sha256"}
  },
  "$defs": {
    "sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "nameArray": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1}
    },
    "marginMap": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {"type": "number", "minimum": 0, "maximum": 1}
    },
    "objective": {
      "type": "object",
      "additionalProperties": false,
      "required": ["metric", "minimum_improvement"],
      "properties": {
        "metric": {"type": "string", "minLength": 1},
        "minimum_improvement": {"type": "number", "exclusiveMinimum": 0, "maximum": 1}
      }
    }
  }
}
